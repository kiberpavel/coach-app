import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    paddingVertical: 10,
  },
  content: {
    justifyContent: 'space-around',
    alignItems: 'center',
    flex: 1,
  },
});
